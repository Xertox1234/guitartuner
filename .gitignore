# macOS
.DS_Store

# Xcode — the project is generated from project.yml via XcodeGen.
LUMA.xcodeproj/
*.xcworkspace/xcuserdata/
**/xcuserdata/
DerivedData/
build/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Benchmark CSV is regenerated each run (the committed accuracy.md is the spec).
docs/benchmarks/accuracy.csv
# Stray output if the benchmark is run from inside the package dir.
Packages/*/docs/
